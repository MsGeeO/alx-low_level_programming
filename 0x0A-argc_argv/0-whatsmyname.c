#include <stdio.h>

/**
 * main - this is function to print its name
 * @argc: amount of arguments passed through the program
 * @argv: pointer that contains the arrys of the arguments
 * Return: 0 for success
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
